import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const primaryDark = Color(0xFF10242F);
  static const deepTeal = Color(0xFF1B3A3A);
  static const accentGreen = Color(0xFF40D2BA);
  static const softWhite = Color(0xFFF7FFFF);

  static const aiBubble = Color(0xFF1E2F3A);
  static const userBubble = accentGreen;

  static const glassOverlay = Color.fromRGBO(255, 255, 255, 0.08);
}
